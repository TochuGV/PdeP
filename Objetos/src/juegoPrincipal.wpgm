import wollok.game.*
import pantallas.handlerPantalla.*

program juegoPrincipal {
  game.cellSize(50);
  game.width(1900 / 50);
  game.height(1000 / 50);
  game.title('Switch!');
  handlerPantalla.inicializar();
  game.start();
}