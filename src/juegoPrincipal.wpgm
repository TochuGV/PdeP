import wollok.game.*
import juego.*

program juegoPrincipal {
  game.cellSize(50);
  game.width(1100 / 50); // 1080 / 5
  game.height(550 / 50); // 540 / 5
  game.title('Switch!');
  juego.iniciar();
  game.start();
}