import wollok.game.*
import juego.*

program juegoPrincipal {
  game.cellSize(3);
  game.width(1080 / 3); // 1080 / 5
  game.height(540 / 3); // 540 / 5
  game.title('Switch!');
  juego.iniciar();
  game.start();
}